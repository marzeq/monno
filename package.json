{
  "scripts": {
    "prod": "rimraf dist/ && tsc && ENV=prod node dist/index.js",
    "dev": "rimraf dist/ && tsc && ENV=dev node dist/index.js"
  },
  "dependencies": {
    "discord.js": "^13.3.1",
    "dotenv": "^10.0.0",
    "lodash.merge": "^4.6.2"
  },
  "devDependencies": {
    "@types/lodash.merge": "^4.6.6",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
